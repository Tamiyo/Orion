// RUN: %yuzu-compile --debug-hir %s 2>&1 | %FileCheck %s --implicit-check-not=error:
//
// A nested function may reference the enclosing type parameter.
fn outer[T](x: T) -> T {
  fn helper[U](u: U) -> U {
    let z: T = x
    return u
  }
  return x
}
// CHECK: FuncStmt
